version: '3'
services:
    db:
        image: mysql:5.7
        environment:
            MYSQL_ROOT_PASSWORD: kota1171997117 # 後の作業で必要になってくる情報
            MYSQL_DATABASE: root
        ports:
            - "3306:3306"

    web:
        build: .
        command: bundle exec rails s -p 3000 -b '0.0.0.0'
        volumes:
            - .:/kochanapp
        ports:
            - "3000:3000"
        links:
            - db