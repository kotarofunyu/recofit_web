<h1>記録一覧</h1>
<p>今日、記録をつけましたか？</p>
<%= link_to '記録する', '/post' %>
<ul>
    <%= @records.each do |record| %>
    <li>
        <dl>
            <dt>日にち</dt>
            <dd><%= record.created_at.strftime("%Y-%m-%d %H:%M") %></dd>
        </dl>
        <dl>
            <dt>種目</dt>
            <dd><%= record.menu %></dd>
        </dl>
        <dl>
            <dt>重量</dt>
            <dd><%= record.weight %>kg</dd>
        </dl>
        <dl>
            <dt>レップ数</dt>
            <dd><%= record.repitition %>レップ</dd>
        </dl>
        <dl>
            <dt>セット数</dt>
            <dd><%= record.sets %>セット</dd>
        </dl>
        <dl>
            <dt>コメント</dt>
            <dd><%= record.comment %></dd>
        </dl>
        <dl>
            <dt>画像</dt>
            <dd><%= image_tag record.picture.url if record.picture? %></dd>
        </dl>
    </li>
    <% end%>
</ul>